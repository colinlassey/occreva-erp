export default function HomePage(): JSX.Element {
  return (
    <section className="space-y-4">
      <h2 className="text-2xl font-semibold">Welcome to Occreva ERP</h2>
      <p className="text-slate-600">
        Bootstrap slice complete. Next slice should implement Auth + Workspace + RBAC framework.
      </p>
    </section>
  );
}
